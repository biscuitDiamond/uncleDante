
import { IComboItem } from "../../models"
import { combos } from "../../data/dataEng"
export function Combo(){
    return(
        <div>
            {combos.map(c =>
                <div>
                    
                </div>
            )}
        </div>
    )
}